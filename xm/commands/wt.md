---
description: Session worktree — isolate the whole session in a git worktree, then land it back to the parent branch
---

User provided: $ARGUMENTS

Invoke the `wt` skill to handle this request. Follow the instructions in `skills/wt/SKILL.md` (bundled with this plugin) and apply them to the user's arguments above.
