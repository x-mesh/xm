---
description: Check whether existing tests detect bounded source mutations; does not generate tests
---

User provided: $ARGUMENTS

Invoke the `mutate` skill to handle this request. Follow `skills/mutate/SKILL.md` and apply the arguments above.
