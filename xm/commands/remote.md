---
description: Manage Discord-based remote Claude/Codex sessions on Linux hosts
argument-hint: gateway|host|run|sessions|steer|interrupt|resume
allowed-tools: Bash
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/remote/SKILL.md`, then run the matching `xm remote $ARGUMENTS` command. Preserve the safety contract: full-access execution is enabled, but secrets are local-input-only.
