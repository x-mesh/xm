---
description: Release automation — commit squash, version bump, changelog, push. Works with any project.
---

User provided: $ARGUMENTS

Invoke the `ship` skill to handle this request. Follow the instructions in `skills/ship/SKILL.md` (bundled with this plugin) and apply them to the user's arguments above.
