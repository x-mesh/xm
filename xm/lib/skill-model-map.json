{
  "version": 1,
  "comment": "Maps model_profile (economy/default/max) to per-skill leader model. Applied via skill-frontmatter-sync.mjs to source SKILL.md frontmatter (model: field). Sub-step Agent({model}) routing is independent — see agent-model.mjs.",
  "policy": {
    "economy": "Sonnet 중심, 기계적 스킬은 haiku",
    "default": "합리적 기본 — 단순 스킬 haiku, 추론 sonnet",
    "max": "품질 우선 — 추론 스킬은 opus, 순수 기계적 작업만 haiku 유지"
  },
  "skills": {
    "handoff":   { "economy": "haiku",  "default": "haiku",  "max": "haiku",  "rationale": "pure delegate — CLI does all work" },
    "handon":    { "economy": "haiku",  "default": "haiku",  "max": "haiku",  "rationale": "pure delegate — JSON read + display" },
    "later":     { "economy": "haiku",  "default": "haiku",  "max": "haiku",  "rationale": "pure delegate — CLI queue ops + display" },
    "dashboard": { "economy": "haiku",  "default": "haiku",  "max": "haiku",  "rationale": "mechanical display, no reasoning" },
    "memory":    { "economy": "haiku",  "default": "sonnet", "max": "opus",   "rationale": "retrieval + reasoning over context" },
    "trace":     { "economy": "haiku",  "default": "sonnet", "max": "opus",   "rationale": "trace analysis benefits from deeper model" },
    "humble":    { "economy": "haiku",  "default": "sonnet", "max": "opus",   "rationale": "retrospective synthesis" },
    "kit":       { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "orchestration entrypoint, mixed commands" },
    "build":     { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "planning + DAG reasoning" },
    "op":        { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "strategy orchestration" },
    "solver":    { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "structured problem solving" },
    "ship":      { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "release decisions, commit message authoring" },
    "review":    { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "code review depth scales with model" },
    "eval":      { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "quality scoring calibration" },
    "probe":     { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "investigation reasoning" },
    "agent":     { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "agent orchestration primitives" },
    "sync":      { "economy": "sonnet", "default": "sonnet", "max": "opus",   "rationale": "interactive setup wizard" }
  }
}
