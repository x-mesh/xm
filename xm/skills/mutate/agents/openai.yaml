interface:
  display_name: "xm mutate"
  short_description: "Check whether existing tests detect mutations"
  default_prompt: "Use $xm:mutate to check changed lines for surviving mutants with external mutation tools, without generating tests."
policy:
  allow_implicit_invocation: false
