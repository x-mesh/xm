# Runtime data (created per-project, not part of plugin)
.xm-build/
.tm-build/
.omc/

# xm-build runtime (projects, metrics — created during use)
xm-build/lib/projects/
xm-build/lib/metrics/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
