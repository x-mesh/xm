---
description: Convert requirements into a validated PlanEnvelope JSON document
---

User provided: $ARGUMENTS

Invoke the `plan` skill and apply it to the arguments above. This command produces planning JSON only; it never starts x-build execution.
