---
description: Phase-based project harness — manage project lifecycle, DAG execution, cost forecasting, and agent orchestration
---

User provided: $ARGUMENTS

Invoke the `build` skill to handle this request. Follow `skills/build/SKILL.md` and use the CLI's concrete adaptive profile when none is explicit; ask once only when `profile_recommendation.confirmation_required` is true.
